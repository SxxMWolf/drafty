# 4. 진행 로그

커밋 날짜로 주차만 잡았다.

아래 개인 메모는 당시 일기가 없어, 기억과 커밋 흐름을 바탕으로 나중에 채운 것이다.

(주차 구간은 편의상 분류, 커밋 밀도와 정확히 일치하지 않을 수 있음. 예: 2026-02-01 커밋 없음. 02-09~02-15 커밋 없음. 02-16~02-17 커밋 없음 — 다음 커밋은 02-18 `af9c7c4`. 02-20~02-22 커밋 없음. 02-28~03-01 커밋 없음.)

---

## 2026-01-26 ~ 01-31

(커밋 메시지 없음, 파일 diff 기준 추정)

**커밋에서 확인된 변경**

- 확장 + Node/Python 서버 + iOS 키보드 골격 (`555d5d0`)
- OpenAI 연동·엔드포인트 `/api/polish`·`/api/digest` 분리 (`ee04a7d`). UI·API 이름을 Enhance/Extract로 변경 (`7e52f30`, 2026-01-27)
- 팝업 톤 UI (`2d804cc`), Extract 카드 개선(다크모드·글자크기·Retry 등), privacy.html, docs
- Gemini 디버그 스크립트 추가 후 삭제 (`97dd92f` → `857a465`)
- `server.py` 삭제, OpenAI 패키지 정리 (`3d45768`)

**개인 메모**

첫 주에 거의 뼈대를 다 올렸다. localhost에서 Rewrite 버튼만 뜨던 상태에서 Polish/Digest → Enhance/Extract로 이름을 바꿨다. Gemini도 잠깐 돌려보다가 OpenAI로 정리했다. 톤 팝업이랑 Extract 카드를 붙인 뒤부터 “쓸 수 있는 확장”처럼 보이기 시작했다. 대신 응답이 느리고, 결과 말투·줄바꿈이 들쑥날쑥해서 계속 거슬렸다.

---

## 2026-02-02 ~ 02-08

(커밋 메시지 없음, 파일 diff 기준 추정)

**커밋에서 확인된 변경**

- iOS 키보드·서버 수정 (`e243731` 등)
- rate-limit·API 키 미들웨어 등 서버 의존성/로직 변화 (`e243731`)
- 스크린샷 정리 (`dc1b76d`)

**개인 메모**

키보드 쪽을 손보고, 서버에 API 키·rate limit을 넣었다. 아무나 호출하면 안 될 것 같아서. 데모용 화면도 찍었다. 속도 이슈는 여전했다.

---

## 2026-02-09 ~ 02-15

(커밋 메시지 없음, 파일 diff 기준 추정)

**커밋:** 없음

**개인 메모**

커밋은 없다. 수업·다른 일 때문에 손을 거의 못 댔다. Extract 카드가 불편하다는 느낌만 남아 있었다.

---

## 2026-02-16 ~ 02-22

(커밋 메시지 없음, 파일 diff 기준 추정)

**커밋에서 확인된 변경**

- iOS 키보드·서버 수정. `ensureParagraphs` 추가 (`af9c7c4`, 2026-02-18). `7650381`(2026-02-19)

**개인 메모**

줄 띄어쓰기가 과한 출력이 계속 나와서 서버 쪽에 단락 보정을 넣었다. 톤도 Professional인데 캐주얼하게 나올 때가 있어서, 프롬프트만으로는 부족하다고 느꼈다.

---

## 2026-02-23 ~ 03-01

(커밋 메시지 없음, 파일 diff 기준 추정)

**커밋에서 확인된 변경**

- CORS·환경변수·프롬프트/출력 후처리 정리 (`0f43b3d`, `cdcaa2b`, 2026-02-27)
- 확장·키보드 파일 수정 (`0f43b3d`, `cdcaa2b`, `2f2748a`)
- 스토어/데모용 이미지 추가 (`1253135`)

**개인 메모**

CORS를 열어 두고 서버·프롬프트를 한 번 더 정리했다. 스토어 올리는 자료도 모았다. 요약 카드는 Retry·글자 크기까지는 넣었는데, 여전히 “읽기 편하다”까지는 아니다.

---

## 2026-03-02

(커밋 메시지 없음, 파일 diff 기준 추정)

**커밋에서 확인된 변경**

- enhance 시스템 프롬프트 문구 수정 (`069afe3`: rewrite → improve, 톤 지시 추가 등)

**개인 메모**

톤이 안 맞는 경우가 남아서 enhance 프롬프트만 짧게 고쳤다. 이 시점에서 큰 기능 추가는 멈추고 마무리로 봤다.
